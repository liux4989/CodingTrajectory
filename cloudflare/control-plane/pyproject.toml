[project]
name = "coding-trajectory-worker"
version = "0.1.0"
description = "Python Cloudflare authority for CodingTrajectory"
requires-python = ">=3.14,<3.15"
dependencies = ["pydantic==2.12.5"]

[dependency-groups]
dev = [
    "uv==0.12.9",
    "workers-py==1.17.4",
    "workers-runtime-sdk==1.9.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = []
