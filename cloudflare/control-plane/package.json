{
  "name": "@coding-trajectory/control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "contracts": "uv run --frozen python ../../scripts/bundle-worker-contracts.py",
    "check": "npm run contracts && uv run --frozen pywrangler sync && uv run --frozen python ../../scripts/check-python-worker.py",
    "dev": "npm run contracts && uv run --frozen pywrangler dev",
    "deploy": "uv run --project ../.. python ../../scripts/deploy-release.py"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
