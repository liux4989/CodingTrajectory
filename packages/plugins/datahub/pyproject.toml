[project]
name = "ct-plugin-datahub"
version = "0.1.0"
description = "Project and session data hub plugin for CodingTrajectory"
requires-python = ">=3.12"
dependencies = [
    "coding-trajectory-core",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
coding-trajectory-core = { workspace = true }
