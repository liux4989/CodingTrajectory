[project]
name = "ct-plugin-dashboard"
version = "0.1.0"
description = "Project and session dashboard plugin for CodingTrajectory"
requires-python = ">=3.12"
dependencies = [
    "coding-trajectory-core",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
coding-trajectory-core = { workspace = true }
