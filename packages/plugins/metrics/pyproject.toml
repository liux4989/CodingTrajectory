[project]
name = "ct-plugin-metrics"
version = "0.1.0"
description = "Canonical session metrics comparison plugin for CodingTrajectory"
requires-python = ">=3.12"
dependencies = [
    "coding-trajectory-core",
    "pydantic>=2.12",
]

[tool.uv.sources]
coding-trajectory-core = { workspace = true }
