[project]
name = "coding-trajectory-core"
version = "0.1.0"
description = "Core models, ingestion, and query layer for coding agent session graph data"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "coding-trajectory-plugins",
]

[tool.uv.sources]
coding-trajectory-plugins = { workspace = true }

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/coding_trajectory"]
