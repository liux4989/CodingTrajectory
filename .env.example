# Optional Cloudflare control-plane fallback. Tokens are scoped by the Worker.
# .env is ignored and must never be committed.
CT_CLOUDFLARE_URL=
CT_ACCESS_TOKEN=
CT_REMOTE_WORKSPACE_ID=
CT_COLLECTOR_AGENT_ID=
