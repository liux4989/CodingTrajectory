[project]
name = "coding-trajectory-workspace"
version = "0.1.0"
description = "Unified Coding Agent Trajectory & Metrics Platform"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/*", "benchmarks"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
